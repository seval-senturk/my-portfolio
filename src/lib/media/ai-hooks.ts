/**
 * Future AI enrichment entry points for the media system.
 * Register implementations when AI alt text, tagging, or categorization is enabled.
 */
export { mediaAiEnrichmentHooks } from "@/services/media/media.service";
export type { MediaAiEnrichmentHooks } from "@/types/media-management";
